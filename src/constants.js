import { Dimensions } from "react-native"

const bgColor = "#eef0f4"
const inputBg = "#fff"
const lightText = "#808080"
const windowHeight=Dimensions.get('window').height
const windowWidth=Dimensions.get('window').width



export { 
    bgColor, 
    windowHeight, 
    windowWidth, 
    lightText, 
    inputBg, 
 }